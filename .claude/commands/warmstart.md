---
description: Start-of-session warm-start — recall the canon and the open questions that gate today's work.
argument-hint: [arm/module, e.g. HAZ-IBTRACS or "cross-arm integration"]
---
We are starting a working session on **climateCCR**. Today's focus: $ARGUMENTS

Before changing anything:

1. Read `context/00_README_CONTEXT.md`, then `context/DECISIONS.md` and `context/DATA_CONTRACTS.md`
   for the arm/module in focus, and `context/OPEN_QUESTIONS.md` for the gating questions.
2. Recall the relevant decisions by ID and the open questions (`OQ-*`) that gate this work.
3. State whether this is a **deep single-arm** session (CCR / MKT / HAZ in isolation) or a
   **cross-arm integration** session, and summarize the current state in 5–10 lines.
4. Then wait for my go-ahead before editing. At the end of the session I will run `/digest`.
