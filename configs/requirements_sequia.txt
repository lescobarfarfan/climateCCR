# Requisitos del pipeline de sequía (Python 3.10+)
# Instalar con:  pip install -r requirements_sequia.txt
numpy>=1.24
scipy>=1.10
pandas>=2.0
xarray>=2023.1
netCDF4>=1.6
geopandas>=0.13
shapely>=2.0
pyproj>=3.5
cdsapi>=0.7        # API del Climate Data Store (requiere ~/.cdsapirc)
pyarrow>=12.0      # opcional: salida en parquet (si falta, se escribe solo CSV)
