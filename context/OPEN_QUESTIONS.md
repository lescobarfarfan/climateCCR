# OPEN_QUESTIONS — Integrated open items

Only what is genuinely open. When something is resolved, move it to `DECISIONS.md` (dated) and delete it here (or drop it to "Recently resolved" for brief traceability). IDs follow `OQ-<ARM>-NN`; integration questions `OQ-INT-NN` come first because they gate the others.

## Table of contents
- **Integration (decide first)** — `OQ-INT-01`–`OQ-INT-12`
- **CCR arm** — `OQ-CCR-02`–`OQ-CCR-07`
- **MKT arm** — `OQ-MKT-01`–`OQ-MKT-13`
- **HAZ arm** — `OQ-HAZ-01`–`OQ-HAZ-19`

---

## Integration — decide before drafting the methodology chapter

- `OQ-INT-01` ✅ **Largely resolved** by `INT-09`. The unifying question: *find, test, and quantify a relationship between financial asset prices / risk factors and climate events, and measure via Monte Carlo how financial risk changes once climate is incorporated.* HAZ estimates the link; the jump channel injects it; MKT+PIMPA simulate; climateCCR reads out the change. **Residual:** the precise hypothesis wording and the headline empirical claim for the manuscript (couples with `OQ-INT-02`).
- `OQ-INT-02` ✅ **Resolved 2026-07-25** (`INT-23`): the **EE profile family leads** the results chapter, headlined by the book-EPE delta band across the `INT-20` λ band (−8.94/−5.44/−4.86% since the `CCR-RISK-05` audit re-base of 2026-08-12; earlier states: −8.28/−5.00/−4.46 `INT-32`, −8.40/−5.07/−4.53 `MKT-CALIB-08`, −8.36/−5.05/−4.51 `INT-26`, −8.44/−5.12/−4.50 `INT-25`, −8.74/−5.35/−4.68 `INT-24`, −11.0/−6.5/−5.6 uniform); supervisory PFE99 (floored, `CCR-RISK-03`) is the tail secondary, VaR/ES and the aggregate-loss quantile are robustness mentions, CVA is built (`CCR-RISK-06`) with its scenario decomposition as the wrong-way secondary. Residual of `OQ-INT-01` (the precise hypothesis wording) can now be drafted against this metric.
- `OQ-INT-03` ✅ **Fully resolved.** (a)/(b) resolved provisionally by `INT-13` (2026-07-02; the channel is built, `INT-14`/`DC-CCR-SIM-2`): homogeneous Poisson `λ`, **both targets** (HW1F rate + GBM price) with shared event times, jumps independent of the diffusion; richer options (trajectory `λ(t)`, Cox/doubly-stochastic, correlated transmission, cross-target mark dependence) remain **desirable to-dos** reachable through the built interface without engine change; the placeholder `λ`/marks were swapped for HAZ estimates by `INT-16/17`. ✅ **(c) resolved 2026-08-02** (`INT-29`): **deliberate non-mapping** — transition = NGFS, physical = HAZ-estimated; a manuscript alignment note states the shared SSP2 basis and the retraction caveat; the channel-separation rule (NGFS physical narratives never combined with the HAZ jump) is the operational form (`DC-XWALK-6` resolved).
- `OQ-INT-04` ✅ **Resolved 2026-07-25** (`INT-21`): the Mexican book replaces the PIMPA stand-ins — 26 BMV equities (climate-vulnerable core + IPC diversifiers), 30 NAIDs incl. PEMEX/CFE debt-only, TIIE IRS + equity options + fixed-coupon cebures (`CCR-RISK-02`). The BMV small-universe scope risk resolved empirically: 26 names with 9–25y of history survive the min-data rule. Residuals → `OQ-INT-10` (prospectus-verified bond terms, FRN/spread dynamics) and `OQ-INT-11` (sector-differentiated marks).
- `OQ-INT-05` ✅ **Resolved** (`INT-02`): the integrated project keeps the name **`climateCCR`** (distribution, repo, import package). The broad rename to `climrisk` was rejected — that name is already taken by an existing UNAM package.
- `OQ-INT-06` **Language convention at the bilingual boundary.** Public Python APIs in English; Spanish data identifiers verbatim (`INT-07`). Confirm where the boundary sits for new code (e.g. do the HAZ pipelines keep Spanish function names internally, exposing English wrappers?).
- `OQ-INT-07` **Jump-mark estimation — residuals after `INT-17`/`HAZ-STOCH-05` (2026-07-18).** ✅ (a) **the scale — fixed** (`INT-17`): `mark = −β·L/K = −L/K_eff` with `β`=0.174, `K`=22,964, `K_eff`=131,932 MDP-2025, all estimated; loss-to-price route ratified, market-reaction event study demoted to robustness. ✅ (b) **INPC deflation — wired** (`HAZ-STOCH-05`); residual: confirm the INEGI BIE series verbatim vs the World-Bank mirror in `configs/inpc_anual.yaml` (API token; `[INEGI_INPC]`). ✅ (d) **leading variant — `mayores_200mdp`** (+ ciclón robustness). **Still open:** (c) the step-aware `MarkSampler` (`sample` is time-blind) — priority *further lowered* 2026-07-21 (`HAZ-STOCH-06`): the leading-set arrivals drift does not extrapolate across the 2016 regime break and the trajectory-λ(t) injection was dropped from the plan (→ `OQ-HAZ-19`); still relevant for unthresholded sets (severity +23.1%/yr real, p≈1e-31 — registry regime only). The exposure-vs-climate attribution caveat is *strengthened* (`[PielkeLandsea1998]`: the flat cross-regime ciclón bridge suggests part of the pre-2016 growth was reporting density). Couples the α-recompute (`OQ-MKT-12`) and `OQ-INT-04` (the book swap).
- `OQ-INT-08` **Scope realism vs the ~8-month envelope.** The jump-diffusion spine is one coherent deliverable; decide which extensions (CVA, parametric pricing, weather derivatives, the credit overlay, signatures) are in-scope vs appendix vs future work. Re-baseline `notes/plan/PROJECT_PLAN.md`.
- `OQ-INT-10` **Bond desk realism — residuals after `CCR-RISK-04`/`INT-32` (2026-08-08).** ✅ (b) **FRN pricer built** (`CCR-RISK-04`: TIIE28+sobretasa, single-curve simple-forward projection, static discount margin as the shocked credit column). ✅ (a) **partially — six issuers swapped to issuance-verified terms** (`INT-32`: OMA suplemento-primary; FIBRA_UNO/CEMEX/FEMSA/CFE press+IR-verified; provenance URLs in `configs/mexican_book.yaml`). Probe verdict on feasibility: BMV `docs-pub` prospectus PDFs are bot-blocked (302 → `no_disponible`) while issuer-IR/press pages work — **hand-collection viable, scraping not**; DataBursatil (`databursatil.com`) stays the untested API candidate, Valmer/PIP the quote-vendor route. **Still open:** the remaining representative rows stay `[eng]` — extend verification opportunistically, series by series; the PEMEX sobretasa confirmation (`[eng]`, no public doc found); UDI-linked (real-coupon) series excluded by scope — revisit only if an inflation leg ever enters; credit-spread *dynamics* (stochastic spreads) remain future work, coupling the built CVA's stochastic-WWR residual (`CCR-RISK-06`) and the phased-application question (`OQ-INT-12`). [eng]
- `OQ-INT-11` **Sector/peril-differentiated marks — residuals after `INT-26`/`INT-27` (2026-08-01).** ✅ Phase A adopted (`INT-24`); ✅ Phase B peril-typed events adopted (`INT-25`, incl. industrials G + S-tier sensitivity); ✅ **(e) per-peril severity adopted** (`INT-26`: mean-matched per-label σ, ciclón 1.33 at trigger grain — the σ≈2.9 concern was sub-threshold dispersion; band −8.36/−5.05/−4.51%); ✅ **(b) Phase C executed under its pre-registered gate → FALLA** (`INT-27`: τ=+0.198, p_boot=.94 over 63 cyclone episodes × 26 names — the equity cross-section does not reprice cyclone events; the ordering rests on construction evidence, the `INT-18/19` pattern). ✅ **(f) storm-clustered refit executed 2026-08-01** (`INT-28`: band −7.85/−3.74/−4.63 vs adopted −8.36/−5.05/−4.51 — headline/floor within ±1pp, the CT anchor is the grain-sensitive leg, tails ease; the adopted band stands); ✅ **(g) MDD triage resolved 2026-08-01** (`HAZ-CENAPRED-12`: user rule — `danio_mdd` discarded, MDP dispositions 2/11/3/23/2, the two 2003 fire rows conservatively excluded from unthresholded diagnostics). **Residual (future work under the `[Bressan2024]` coarse-proxy caveat):** within-state asset resolution (LitPop/CLIMADA) and I-O indirect effects (`[Hallegatte2008]`). Couples `INT-23` (band numbers) and `DC-XWALK-4`.
- `OQ-INT-12` **In-simulation / phased scenario application — the scheduled-shock extension of classic Monte-Carlo CCR (user-flagged priority future step, 2026-08-07).** Today every transition shock is an instantaneous t=0 state (`INT-12` fixed flavor; the `MKT-NGFS-09` trajectory flavor moves the *curve* through maturity-dated f(0,t) absorption but still reprices at t=0). The robust extension: apply scenario paths **inside the simulation** — time-dependent drift / scheduled marks at simulation dates (equity/spread legs phased along their NGFS paths, policy-rate steps as scheduled θ(t) perturbations), which also re-opens the trajectory-λ(t) physical injection (`OQ-INT-03` c residual, `OQ-HAZ-19`) and its deferred visuals (ex-`OQ-GEN-02` c). Interface-ready per `INT-13` (the jump hook already takes marks at dates); the engine's scenario evaluation would need a scheduled-shock seam — design before build, gate per `GEN-31`. [eng]

## CCR arm

- `OQ-CCR-02` **Tidy time-series schema, final form.** Ratify the `PROPOSED` columns in `DATA_CONTRACTS.md` `DC-CONV-9`/`DC-CCR-DATA-1`; decide whether a wide convenience view is needed and how curve tenors are encoded.
- `OQ-CCR-05` **RQ1 design details:** climate-event labelling / narrative signal (event-window definition, scenario tags); the leakage-aware walk-forward CV split; and a **pre-registered go/no-go criterion** for Path A vs Path B (decide *before* seeing results to avoid hindsight bias). Most open-ended; gates RQ1.
- `OQ-CCR-07` **Where the randomized signatures fit, now that HAZ carries the empirical climate→price link (`INT-11`).** Options: (a) a **complementary detection/validation** method — use signatures to test whether a climate signal is present in price series independently of the hazard panels; (b) a **robustness probe** on the jump channel; (c) repositioned to future work. Decide before investing in fixing/extending the reservoir beyond the `CODE_REVIEW` repairs. Couples with `OQ-INT-02/08`.
## MKT arm

- `OQ-MKT-01` **SIE compounding convention — final empirical confirmation.** Decision is simple-interest Act/360 (`MKT-SIE-04`); run the documented check (plug published `r^F_T` into both forms vs the index ratio) once on real data and record the result. (2026-07-20: not runnable yet — the CA766 compounded tenors are not in the downloaded set; add their series ids to `configs/sie_series.yaml` first, `DC-MKT-SIE-5`.)
- `OQ-MKT-02` **λ estimation for Mexico.** Method/data for the market price of interest-rate risk are not fixed (historical excess-return regression vs Yasuoka's forward-rate approach). Affects all P-measure stress output. Also confirm `[Yasuoka2018]`/`[JamesWebber2000]` editions (residual re-homed from the closed `OQ-MKT-05`). (← `MKT-MEAS-01`)
- `OQ-MKT-03` **Long-end densification.** Only two pillars in 10–30y; decide whether to pull off-the-run Bonos M from CF300, and for which portfolios it's necessary. (← `MKT-CURVE-04`; quantified 2026-07-20: NS rmse 12 bp on the 10-pillar 2026-07-17 strip, `MKT-CURVE-05`. **Svensson evaluated and rejected 2026-08-02** — rmse only 12.0→10.3 bp because the miss is the 2-pillar 10–30y stretch, not curvature (`MKT-CURVE-06`) — so densification is the only remaining route.)
- `OQ-MKT-04` **NGFS scenario-vintage mix — deferred until the long-term vintage joins** (`MKT-NGFS-04`: the ST-only first build needs no splice). When the Phase-V/VI LT build starts: confirm the splice point (ST quarterly 2025–2030 → LT annual beyond) and how to blend the two rate paths without a kink — two different model worlds (GEM-E3/EIRIN vs NiGEM+IAM), no official NGFS splice guidance. (← `MKT-NGFS-03`)
- `OQ-MKT-07` **Weather derivatives — chapter or context?** Confirm whether this becomes an applied pricing/hedging chapter (temperature OU model on Mexican stations) or remains literature review. (← `MKT-WD-01`)
- `OQ-MKT-08` **R1–R4 band definitions.** The dashboard hazard risk-level thresholds (what distinguishes R1 from R4 per hazard) are unspecified — needed for reproducibility and heat-map cutoffs.
- `OQ-MKT-09` **Hazard data source for Mexican municipalities.** Which dataset populates `fenomeno × año × escenario` at `clave_mpio` grain (ATLAS Nacional de Riesgos / CMIP6 downscaled / commercial)? (Couples with the HAZ pipelines — possible reuse.)
- `OQ-MKT-10` **`industria` ↔ GICS/SCIAN crosswalk.** The dashboard's `industria` field needs a fixed mapping to GICS (carbon intensity) and/or SCIAN (Mexican classification) for the credit overlay. (← `DC-MKT-CREDIT-5`)
- `OQ-MKT-11` **Is the climate-credit overlay (Strand C) in scope, or context only?** It bridges the rate and physical strands but adds substantial data burden (emissions, geolocation, CDS). (Couples with `OQ-INT-02`.)
- `OQ-MKT-13` **NGFS connector extensions (post-`MKT-NGFS-04..08`/`INT-29/30/31`).** ✅ (c) equity/corporate leg, (d) `[BanxicoREF]` pin, (e) inner-band reordering — **all resolved 2026-08-03** (`MKT-NGFS-08`/`INT-31`: sector-grain S0 + excl-policy spread overlays with sign-flipping full-leg deltas; REF dic-2024 Recuadro 6 pinned; the "reordering" was an `INT-30` (b) transcription artifact, corrected in place). ✅ (a) **trajectory flavor resolved 2026-08-08** (`MKT-NGFS-09`: maturity-dated anchor deltas, flat-path reduction invariant, hold-constant beyond the window; rates-only — the phased equity/spread application re-homed to `OQ-INT-12`; nivel stays headline, trajectory = robustness flavor). **Still open:** (b) the **long-term vintage join** (Phase V now / Phase VI on release per `GEN-31`) — reopens the `OQ-MKT-04` splice and brings Current-Policies baselining and NiGEM country-grain rate variables.

## HAZ arm

- `OQ-HAZ-01` **CDMX discretization artifact** in the IBTrACS wind-field panel — CDMX drops out (smaller than a 0.5° cell). Fix with a finer grid or point-based attribution for small entities. (← `HAZ-IBTRACS-10`)
- `OQ-HAZ-02` **Exact value separating SIPAC in autos** — locate the cut (sentinel/threshold) that distinguishes the SIPAC block.
- `OQ-HAZ-03` **`MONEDA` normalization (CNSF)** — requires Banxico FX; now wireable from the MKT/SIE pipeline (`DC-XWALK-5`). (← `HAZ-CLEAN-CNSF-10`) → **Evidence 2026-07-18** (`HAZ-CLEAN-CNSF-14`): SESA *amounts* are MXN regardless of `MONEDA` (Odile 2014 cross-check; coded in `annual_real_amounts`), so flow amounts need no FX conversion. Residual: external/documentary confirmation (`[AMIS2014Odile]`) and whether any suma-asegurada use requires per-policy currency treatment.
- `OQ-HAZ-04` **Confirm the year convention in CNSF** — verify that **year of occurrence** (`OCURRIDO`), not of report, is used in each sector.
- `OQ-HAZ-05` **Exact list of agro-sector causes** — confirm against the real catalogs. (← `HAZ-CLEAN-CNSF-11`)
- `OQ-HAZ-06` **Validate IBTrACS covariates** against known events (Odile 2014 in BCS, Otis 2023 in Guerrero) — run on the author's machine. (← `HAZ-IBTRACS-07/09`)
- `OQ-HAZ-07` **Stochastic textbooks/papers to confirm:** `[Baryshnikov2001]`, `[Burnecki2005]`. (see `REFERENCES.md` §99; `[Klugman]` pinned 2026-07-25 as `[Klugman2019]`, 5th ed.)
- `OQ-HAZ-08` **CLIMADA flood-route references:** exact **Hazus** manual edition and **`[Wagenaar2018]`** journal/DOI.
- `OQ-HAZ-09` **Access/coverage to confirm:** CONAFOR/SNIF, NASA FIRMS-MODIS, Munich Re NatCatSERVICE / Swiss Re *sigma*, Dartmouth FO / Global Flood Database; re-confirm `[WMO2012SPI]` URL on access.
- `OQ-HAZ-10` **Timestep convergence test** (1 h default) before freezing the CLIMADA hazards. (← `HAZ-CLIMADA-10`)
- `OQ-HAZ-11` **Wind-radii attribution refinement** vs the fixed buffer (future improvement). (← `HAZ-IBTRACS-07`)
- `OQ-HAZ-12` **Stochastic-model calibration — residual routes.** ✅ **Compound-Poisson route built** (`INT-16`/`HAZ-STOCH-04`, 2026-07-16: Poisson `λ` + log-linear trend, lognormal severity, from CENAPRED). **Still open:** Cox / doubly-stochastic `λ(t)` on covariates (the IBTrACS wind panel, `DC-HAZ-IBTRACS-3`; interface-ready per `INT-13`); the CNSF insured-loss route as the cross-check (`HAZ-CLIMADA-12` pattern, `HAZ-STOCH-01`); tail refinement beyond lognormal (POT-GPD on CENAPRED per-event losses). (Couples `OQ-INT-07`.)
- `OQ-HAZ-13` **Parametric-instrument pricing** — downstream HAZ deliverable.
- `OQ-HAZ-14` **Integration/testing of the remaining CNSF sectors** through the scraper pipeline.
- `OQ-HAZ-17` **Agro magnitude-correction residuals** (`HAZ-CLEAN-CNSF-12/13`): (a) manual review of the **132 weak-signature rows** (prima ≤ 0, no confirming own history — e.g. arándano/agave "por planta" with `UNIDADES ASEGURADAS` = 0) deliberately left uncorrected in the copies (the corrector lists them on each run); (b) external confirmation of the systemic ×FIX error (CNSF fe de erratas / INAI request; insurer attribution is impossible from the files); (c) intra-year FIX refinement — the annual-average ÷FIX carries ±5–10% level uncertainty in 2022–2024 (per-row issue-date FIX would remove it; the FX series is wireable via `DC-XWALK-5`); (d) the standing triage queue: `error_probable` findings on the corrected emisión (323) beyond the corrected rows, in `results/inspeccion/` (`GEN-27`).
- `OQ-HAZ-19` **Post-extension residuals (2026-07-21; successor of `OQ-HAZ-18` after `HAZ-CENAPRED-10`/`HAZ-STOCH-06`/`INT-19`).** ✅ (a) **the headline arrival intensity — resolved 2026-07-25** (`INT-20`): registry λ=19.2857/yr headline (measurement-consistent with the `INT-17` scale window) + a regime-consistent scenario band as robustness (report floor 7.2222, CT anchor 9.9565; `configs/climate_jump_real_{floor,ct_anchor}.yaml`). **Still open:** (b) Replace the preliminary 2024 rows when the *Impacto Socioeconómico 2024* extenso appears and refit (the 2016–2023 twin barely moves: λ 7.6 vs 7.2). (c) The non-CT stream post-2016 is censored (LLUV/sequía as state-annual aggregates) — if a future CENAPRED product restores event grain, refit; until then cross-regime statements ride on the ciclón bridge. [eng]

---

## Recently resolved (traceability)

- ~~Whether the three arms relate~~ → framed: CCR is the spine, MKT and HAZ feed it (`INT-01`); the **research narrative** is now also resolved (`INT-09/10/11`): one machine that tests the price↔climate relationship via a HAZ-calibrated jump injected into GBM/HW1F diffusions, read out by Monte Carlo.
- ~~How the HAZ panels reach the financial engine~~ → the **climate jump channel** (`INT-10`, `DC-CCR-SIM-2`, `DC-XWALK-4`): `λ` + impact → jump-diffusion. Residual estimation detail is `OQ-INT-07`.
- ~~Path A vs Path B framing~~ → subsumed by `INT-12`: HAZ→jump = concrete Path A; fixed parameter shift = Path B; both via one injection hook. Signatures' role → `OQ-CCR-07`.
- ~~Package name should change~~ → **keep `climateCCR`** for the integrated project (`INT-02`); the broad rename to `climrisk` was rejected (name already taken by a UNAM package).
- ~~CCR risk-evaluator API name~~ → keep `CCR_Valuation_Session` verbatim, behaviour-unchanged migration (`CCR-MIG-05`, resolves `OQ-CCR-01`).
- ~~Route the MC draw through `infra` — preserve stream or re-baseline?~~ → **preserve the stream** via `infra.get_legacy_rng` (`RandomState(seed)` ≡ SciPy int-seed); baseline unchanged (`CCR-MIG-08`, resolves `OQ-CCR-09`).
- ~~Jump-channel knobs: which target, what dependence, Poisson vs Cox (`OQ-INT-03` a/b)~~ → fixed provisionally and **built** (2026-07-02): homogeneous Poisson, both targets with shared events, independent of the diffusion; trajectory `λ(t)` / Cox / correlated transmission kept as interface-ready to-dos (`INT-13/14`, `DC-CCR-SIM-2`). Residuals stay in `OQ-INT-03` (c) + `OQ-INT-07`.
- ~~Meaning of `NU` (CNSF)~~ → unlocated → `No Disponible` (`HAZ-CLEAN-CNSF-01`).
- ~~CENAPRED vs CLIMADA output structures differ~~ → they do; processor emits both A + B (`HAZ-CENAPRED-02`).
- ~~Pressure-governed Holland-profile bug~~ → anchor to Vmax (`HAZ-IBTRACS-04`).
- ~~Drought-index choice~~ → SPEI primary, SPI robustness, MSM complementary (`HAZ-DROUGHT-01`).
- ~~NGFS policy rate used as a level~~ → used as a shock source (`MKT-NGFS-01`).
- ~~1-year curve gap~~ → 364-day Cetes pillar (`MKT-CURVE-01`).
- ~~CNSF test-harness import bug (`OQ-HAZ-15`)~~ → resolved (`HAZ-SCRAPER-CNSF-09`, 2026-07-11): root-conftest `sys.path` shim + origin-sandbox portability fixes; tests pass against the real 2024 Incendio sample from `data/hazard_mx` (`GEN-24`); collection-safe without the `[haz]` extra.
- ~~Vault formatting back-fill — (a) note math, (c) stray files, (d) mangled pushed messages~~ → done/closed 2026-07-11; `OQ-GEN-01` slimmed to the canon unwrap (a `/compact-canon` task).
- ~~Jump-channel `λ`/marks are placeholders; stochastic calibration not started (`OQ-HAZ-12` original form)~~ → **first real estimates 2026-07-16** (`INT-16`, `HAZ-STOCH-04`): per-variant Poisson `λ` + trend and lognormal `σ` from CENAPRED; residuals = the loss→mark scale `K`, INPC deflation, step-aware marks (`OQ-INT-07`) and the Cox/CNSF/tail routes (`OQ-HAZ-12`).
- ~~The loss→mark scale `K`, INPC deflation, and the leading variant (`OQ-INT-07` a/b/d)~~ → **fixed and estimated 2026-07-18** (`INT-17`, `HAZ-STOCH-05`): `β`=0.174, `K`=22,964 MDP-2025, `K_eff`=131,932; deflation at load time (real 2025 pesos, threshold-consistent), leading variant `mayores_200mdp` (+8.4%/yr survives deflation); first all-estimated EE/PE run (`configs/climate_jump_real.yaml`). The loss→rate translation became `OQ-INT-09`; INEGI-series confirmation and step-aware marks stay in `OQ-INT-07`.
- ~~The loss→rate translation (`OQ-INT-09`, the binding gap)~~ → **resolved 2026-07-19** (`INT-18`): pre-registered event study on SIE yields around 94 CENAPRED episodes → **null** (lead `BonosM_20_30` p=.946; Mexican yields bound the per-event response near zero — the empirical result); per the blind-fixed gate, `rate_marks` carries the `[Anyfantaki2025]` literature **scenario** (`S_rate_eff`=196,734,670 MDP-2025, +1 sign), EE/PE now moves the IRS book. Residuals: the 2016–2024 re-run with Otis (`OQ-HAZ-18`) and the Mexican-`α` recompute + book swap (`OQ-MKT-12`, `OQ-INT-04`).
- ~~Stripping scope — all six buckets vs `Bonos_0_3` only (`OQ-MKT-06`)~~ → **full-curve strip executed 2026-07-20** (`MKT-CURVE-05`): every live bucket bootstraps outward from dirty prices; the 2008-01-23 worked check passes on real data as an integration test.
- ~~Execute the Mexican market calibration, (a) rates + (b) stocks (`OQ-MKT-12` a/b)~~ → **done 2026-07-20** (`MKT-CALIB-05/07`, `MKT-CURVE-05`, `DC-MKT-CAL-1`): HW1F `a`=0.122/`σ`=0.0110 (F-TIIE 2006–2026, MLE, crisis-excluded; full window table), MXN curve + NS + `θ(t)` @ 2026-07-17, IPC GBM `μ`=0.110/`σ`=0.167 — all emitted `'direct_input'`-compatible. The `MKT-CALIB-02` cross-check *fails* informatively → finding + handling rule `MKT-CALIB-06`; residual = the (c) recomputes, kept in `OQ-MKT-12`.
- ~~Extend CENAPRED 2016–2024, refit `λ`/severity, re-run the rate-leg event study (`OQ-HAZ-18`)~~ → **done 2026-07-21** (`HAZ-CENAPRED-10`, `HAZ-STOCH-06`, `INT-19`): extension inspected and accepted (publication-regime break documented — registry → annual reports); regime-aware refits show the +8.4%/yr arrivals growth is regime/window-specific (ciclón bridge flat +1.4%/yr p=.17; report-regime λ=7.2/yr; λ(2025)≈69 extrapolation refuted → homogeneous λ with regime bounds); the pre-registered gate re-applied verbatim on 2002–2024 (n=100 incl. Otis) → **null reconfirmed** (p=.734; the 2016–2024 nominal PASA is Otis leverage — +23 bp reverting to ≈0 in 10 BD); the `[Anyfantaki2025]` literature scenario stands. Residuals → `OQ-HAZ-19` (headline λ, 2024 extenso, censored non-CT stream) and `OQ-MKT-12` (α-recompute).
- ~~Which arrival intensity the headline EE/PE run uses (`OQ-HAZ-19` a)~~ → **resolved 2026-07-25** (`INT-20`): registry λ=19.2857/yr headline (measurement-consistent with the `INT-17` scale) + a regime-consistent scenario band as robustness (report floor 7.2222 × its own severity fit; CT anchor 9.9565 × registry marks); band EE/PE runs executed and reproduced on `main` (NAID 23 mean EE shift +1.11 / +0.57 / +0.48); the Mexican-`a` `S_rate_eff` recompute explicitly deferred to the book swap (`OQ-MKT-12` c).
- ~~Which metric leads the results chapter (`OQ-INT-02`) and the PE floor convention (`OQ-CCR-08`)~~ → **resolved 2026-07-25** (`INT-23`, `CCR-RISK-03`): the EE profile family leads, headlined by the book-EPE delta band (−11.0/−6.5/−5.6%, `viz.epe_summary`); reported tail exposure is supervisory PFE99 = max(quantile, 0), derived at the reporting seam (`viz.with_supervisory_pfe`) — engine raw quantile + golden baselines untouched, floor verified immaterial (29/360 rows, ordering intact). Backed by a verified supervisory/literature pass (Basel CRE, BCBS 2022 Principles/FAQ, BCBS 2021 d517/d518, ECB/EBA, Mexico status; `REFERENCES.md` §11). CVA → future work (`OQ-CCR-04`); sector marks → `OQ-INT-11`.
- ~~The concrete Mexican asset universe replacing the PIMPA stand-ins (`OQ-INT-04`) and the `OQ-MKT-12` (c) recomputes~~ → **resolved 2026-07-25** (`INT-21`/`INT-22`/`CCR-RISK-02`): the Mexican book (26 BMV equities, 30 NAIDs incl. PEMEX/CFE debt-only, TIIE IRS + options + fixed-coupon cebures via the new additive bond desk) built deterministically by `pipelines/09`; `S_rate_eff_MX` = 144,343,565 MDP-2025 (−26.6%); EE/PE band re-baselined on the book (mean EE shift −2,968/−1,705/−1,469 MXN, byte-identical re-runs). New residuals: `OQ-INT-10` (prospectus-verified bond terms; FRN/spread dynamics), `OQ-INT-11` (sector-differentiated marks).
- ~~The sector-differentiated distribution rule itself (`OQ-INT-11` core)~~ → **built and adopted 2026-07-26** (`INT-24`): `γ_i ∝ Σ G·S·D/pob` (hand-collected asset states × sector-peril tiers × CENAPRED per-capita damage), book-anchor renormalized (`Σ wγ = 1`), engine `target_scales` seam, band re-based to EPE −8.74/−5.35/−4.68% (uniform −11.0/−6.5/−5.6 archived); residuals (peril-typed events, event-study validation, industrials G, S-tier sensitivity) stay in `OQ-INT-11`.
- ~~Peril-typed events, industrials G, S-tier sensitivity (`OQ-INT-11` a/c/d)~~ → **built and adopted 2026-07-30** (`INT-25`): shared per-event peril label from the trigger-set frequency mix, `c_ip = γ_i^p/π_p` with the per-name mean preserved exactly; sourced industrials G (HERDEZ stays proxy, documented); jitter + five full-band variants all within ±1pp (`fluvial_merged` ≡ base — the 2016 INUND→LLUV caveat immaterial); band re-based to **−8.44/−5.12/−4.50%** (sector-γ −8.74/−5.35/−4.68 archived as `*_fase_a`). The 2000–2015 base inspección + impactcal-mx QA import logged as `HAZ-CENAPRED-11` (fits uncontaminated: the 2 trigger-set anomaly rows carry corroborated MDP). New residuals (Phase C, per-peril severity, storm clustering, MDD triage review) stay in `OQ-INT-11`.
- ~~Per-peril severity and the Phase C ordering validation (`OQ-INT-11` e/b)~~ → **resolved 2026-08-01** (`INT-26`/`INT-27`): mean-matched per-label severity adopted (ciclón σ=1.3317 at trigger grain — the ≈2.9 was sub-threshold dispersion; band re-based to **−8.36/−5.05/−4.51%**, pooled-severity state archived `*_fase_b`); Phase C executed under its pre-registered gate → **FALLA** (τ=+0.198, p_boot=.94, 63 episodes × 26 names, drop-Otis jackknife agrees) — the γ/c ordering rests on construction evidence, mirroring the `INT-18/19` rate null. Same session: the canonical-env rule `GEN-30` (wrong-env incident caught by the `GEN-06` manifests; baselines re-materialized byte-identical). Residuals (f)/(g) stay in `OQ-INT-11`.
- ~~Storm-clustered row grain + the MDD triage review (`OQ-INT-11` f/g)~~ → **resolved 2026-08-01** (`INT-28`, `HAZ-CENAPRED-12`): the clustered 3-config band holds (−7.85/−3.74/−4.63 vs adopted; CT anchor = the grain-sensitive leg, manuscript note; tails ease; `pipelines/14` + the `cluster_storms` loader seam), and the 41-row triage closed under the user rule (MDD discarded, MDP kept where in-scope/justified, 2 fire rows conservatively excluded — adopted fits untouched). Same session: HAZ data roots re-anchored (`HAZ-SOURCES-04`, closes `OQ-HAZ-16`), the living-calibration direction (`GEN-31`), the FRN-pricer ruling + data-source delivery (`OQ-INT-10`), and the canon-unwrap scheduling (`OQ-GEN-01` → 2026-08-02 pre-NGFS).
- ~~Re-anchor HAZ pipeline default data roots (`OQ-HAZ-16`)~~ → **done 2026-08-01** (`HAZ-SOURCES-04`): 13 legacy defaults across 10 scripts routed through the new `ProjectPaths.data`; sequía keeps its `--raiz` join with the root discovered from repo markers; first-touch ruff+black normalization carried in the same commit.
- ~~The `GEN-31` pre-NGFS evals — weekly `a`/`σ` refit + Svensson upgrade (the `OQ-MKT-12` residual; coupled `OQ-MKT-03`)~~ → **executed 2026-08-02 under pre-registered gates** (`MKT-CALIB-08`/`MKT-CURVE-06`): the weekly refit **adopted** — weekly sampling restores the `MKT-CALIB-02` agreement (0.2% vs 26.9% daily), headline `a`=0.0758/`σ`=0.0081, `S_rate_eff_MX` → 175,293,784 MDP-2025 (`INT-22`), band re-based to **−8.40/−5.07/−4.53%** on the new baseline 269,655.35 MXN (`INT-23`, sub-0.05pp moves — the `MKT-CALIB-06` robustness map confirmed); **Svensson rejected** (rmse 12.0→10.3 bp — long-end sparsity, not curvature); `OQ-MKT-12` closed, `OQ-MKT-03` re-scoped to densification-only.
- ~~Canon hard-wrap unwrap (`OQ-GEN-01`)~~ → **done 2026-08-02** (`GEN-32`): 128 joins across the seven canon files, content-token equality asserted per file, new canon text authored unwrapped.
- ~~The first climate-scenario connector (`OQ-CCR-03`), the Δr baseline (`OQ-MKT-05`), and the NGFS↔SSP stance (`OQ-INT-03` c / `DC-XWALK-6`)~~ → **resolved 2026-08-02** (`MKT-NGFS-04..07`, `INT-29`, `INT-30`): the NGFS **short-term** connector shipped end-to-end (IIASA guest pull with vintage pin → within-package WEO-anchored baseline delta → two-anchor curve shock, Mexico via EIRIN North America + country-grain sovereign adjustment → per-scenario book overlays → EPE readout); rate-only transition deltas HWTP −0.36 % / SWUC +1.50 % / DAPS_NAM +4.28 % (**superseded as headline 2026-08-03**, see next); deliberate NGFS↔SSP non-mapping with the channel-separation rule. Residuals → `OQ-MKT-13` and `OQ-MKT-04` (splice, deferred to the LT join); the `[Yasuoka2018]`/`[JamesWebber2000]` edition confirms re-homed to `OQ-MKT-02`.
- ~~The viz close-out — stage walk, aggregate-loss quantiles, 26-name holdout grid (`OQ-GEN-02` c)~~ → **built 2026-08-08** (`GEN-34`): the `INT-23→26→32` stage walk reproduced to the digit from stored frames; per-path value artifacts (`DC-CCR-RISK-5`) + book-exposure and `[Klugman2019]` compound-Poisson aggregate-loss panels (`pipelines/20`); all-26 holdout cones (PENAVERDE 42% coverage — illiquid step quotes, manuscript caveat). The deferred trajectory-λ(t) visuals re-homed to `OQ-INT-12`; **`OQ-GEN-02` closed** (a+b were `GEN-33`).
- ~~The `OQ-CCR-06` internals audit (IRS pricer / `Curve` / `Surface` / `CorrelationMatrix`)~~ → **done 2026-08-12** (`CCR-RISK-05`): three ACTIVE IRS-pricer errors fixed (missing accrual ×~4, wrong-state forwards, spliced-period fixing), goldens + the live chain deliberately re-based (band → −8.94/−5.44/−4.86% on baseline book EPE 255,217.29 MXN); `Curve`/`Surface`/`CorrelationMatrix` clean and behaviour test-locked; the cc-vs-simple floating convention and the payer sign label → `OQ-CCR-10`.
- ~~Where Effective-EPE / CVA live if built (`OQ-CCR-04`)~~ → **CVA built 2026-08-12** (`CCR-RISK-06`): the thin `risk/ccr/xva.py` at the reporting seam + `pipelines/21` + `configs/cva.yaml`; base BOOK CVA 514,191 MXN (LGD 0.60); the transition legs' credit channel nearly offsets the exposure channel — the `INT-23`/`INT-31` wrong-way caveat quantified; stochastic WWR and Effective-EPE stay future work.
- ~~The IRS floating-rate convention and the payer sign label (`OQ-CCR-10`)~~ → **resolved 2026-08-20** (`CCR-RISK-07`): **simple Act/360 adopted** (both-leg Act/360 money accrual — the `MKT-SIE-04`/FRN convention, closing the ~1.13% cross-desk basis) and **`payer` relabeled to market semantics** (pays fixed) via a byte-identity-gated pure relabel; goldens + full chain re-based — band −8.93/−5.44/−4.86% on baseline book EPE 255,278.60 MXN (+0.024%), NGFS/CVA/aggregate-loss patterns preserved (moves ≤0.16pp) — a consistency fix, not a results change; the `INT-21` NAID-114 sign anchor is now label-consistent with the book data.
- ~~The ST equity/corporate leg, the `[BanxicoREF]` pin, and the inner-band reordering (`OQ-MKT-13` c/d/e)~~ → **resolved 2026-08-03** (`MKT-NGFS-08`, `INT-31`): the CLIMACRED 50-sector families pulled at Mexico grain (PD/WACC included for the future CVA hook) and wired as S0 revaluations + excl-policy cebur spread shocks in the overlays (crosswalk hand-mapped, spread floored at 0, frozen-γ invariant); full-leg transition deltas **HWTP −3.78 / SWUC −3.65 / DAPS_NAM −13.35 %** — the adverse-narrative sign flips, HWTP ≈ SWUC (kink offset), jump-within overlay-invariant, DAPS-vs-`INT-27` as the equity-edition counterpoint; `[BanxicoREF]` pinned (REF dic-2024 Recuadro 6); the "reordering" was an `INT-30` (b) transcription artifact (labels crossed), corrected in place — no real swap, no further study needed. Rate-only readout archived `results/ngfs_epe_readout_solo_tasas`.


---

## Related
Reads with: [[DECISIONS]] (resolved items move there) · [[DATA_CONTRACTS]] (the contracts they gate) · [[WORKFLOW]] (the ritual that closes them). By arm: [[CCR_MOC]] · [[MKT_MOC]] · [[HAZ_MOC]] · Home: [[_INDEX]]
#arm/int #type/open #status/open
